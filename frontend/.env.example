# BravoBot Frontend — Variables de entorno
#
# En Docker (producción): deja vacío → nginx redirige /chat, /health, /categorias al backend
# En desarrollo local (sin Docker): descomenta y usa la URL del backend
#
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
